<% provide(:title, "Sign in") %>
<h1>Sign in</h1>

<%= form_for(:session, url: sessions_path) do |f| %>

	<%= f.label :email, "Sign in" %>
	<%= f.text_field :email %>

	<%= f.label :password %>
	<%= f.password_field :password %>
	
	<%= f.submit "Click to sign in" %>

<% end %>