This is PrivateEvent-User's show.html.erb

You've been created.

<br>
<% if signed_in? && @user == current_user %> 
	You're signed in as "<%= @user.name %>"
<% end %>

<br>
Click <%= link_to "here", new_event_path %> to create a new event.

<br>
<% if @user.events.any? %>
	Yes.
<% else %>
	No events.
<% end %>